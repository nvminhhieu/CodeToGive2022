// frontend routes
const ROUTES = {
  home: "/client/",
  test: "/client/test",
};

const API_ENDPOINTS = {
  test: "",
};

// API urls
const API_BASE_URL = "";

export { ROUTES, API_ENDPOINTS, API_BASE_URL };
