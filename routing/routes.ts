// frontend routes
const ROUTES = {
  home: "/",
  test: "/test",
  assessments: "/assessments",
  login: "/form",
}

const API_ENDPOINTS = {
  test: "",
}

// API urls
const API_BASE_URL = ""

export { ROUTES, API_ENDPOINTS, API_BASE_URL }
