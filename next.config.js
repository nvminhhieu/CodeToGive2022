/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  output: "standalone",
  env: {
    HOST: process.env.HOST,
  },
}

module.exports = nextConfig
